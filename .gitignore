/target
/assets/extracted/
/assets/zips/

# secrets — API keys etc. NEVER commit
.env
.env.*
!.env.example

# Python bytecode
__pycache__/
*.pyc
*.pyo

# Editor-generated voxel + biome state. Reconstructed on demand via
# vaern-editor; pushing the binary blobs hits GitHub's 100 MB limit.
src/generated/world/voxel_edits.bin
src/generated/world/biome_overrides.bin
src/generated/world/elevation_overrides.bin
